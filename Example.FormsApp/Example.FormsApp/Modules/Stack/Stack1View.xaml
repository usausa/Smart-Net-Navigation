<?xml version="1.0" encoding="UTF-8"?>
<ContentView x:Class="Example.FormsApp.Modules.Stack.Stack1View"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:resolver="clr-namespace:Smart.Forms.Resolver;assembly=Smart.Forms"
             xmlns:stack="clr-namespace:Example.FormsApp.Modules.Stack;assembly=Example.FormsApp"
             xmlns:views="clr-namespace:Example.FormsApp.Views;assembly=Example.FormsApp"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type={x:Type stack:Stack1ViewModel}}"
             resolver:BindingContextResolver.Type="{x:Type stack:Stack1ViewModel}"
             views:ViewProperty.Title="Stack1">
    <StackLayout>
        <Button Text="Cancel" Style="{StaticResource NavigationButton}"
                Command="{Binding Forward}" CommandParameter="{views:ViewId Menu}"/>
        <Button Text="Push" Style="{StaticResource NavigationButton}"
                Command="{Binding Push}" CommandParameter="{views:ViewId Stack2}"/>
    </StackLayout>
</ContentView>