<UserControl x:Class="Example.WindowsApp.Views.Stack.Stack1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Example.WindowsApp.Views.Stack"
             xmlns:resolver="clr-namespace:Smart.Windows.Resolver;assembly=Smart.Windows"
             xmlns:views="clr-namespace:Example.WindowsApp.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type={x:Type local:Stack1ViewModel}}"
             resolver:DataContextResolver.Type="{x:Type local:Stack1ViewModel}"
             d:DesignHeight="320" d:DesignWidth="480">
    <Grid>
        <Button Content="Cancel" HorizontalAlignment="Left" Margin="94,58,0,0" VerticalAlignment="Top" Width="75"
                Command="{Binding Forward}" CommandParameter="{x:Type views:MenuView}"/>
        <Button Content="Push" HorizontalAlignment="Left" Margin="289,58,0,0" VerticalAlignment="Top" Width="75"
                Command="{Binding Push}" CommandParameter="{x:Type local:Stack2View}"/>
    </Grid>
</UserControl>
