<UserControl x:Class="Example.WindowsApp.Modules.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:resolver="clr-namespace:Smart.Windows.Resolver;assembly=Smart.Windows"
             xmlns:local="clr-namespace:Example.WindowsApp.Modules"
             xmlns:stack="clr-namespace:Example.WindowsApp.Modules.Stack"
             xmlns:wizard="clr-namespace:Example.WindowsApp.Modules.Wizard"
             xmlns:views="clr-namespace:Example.WindowsApp.Views"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="480"
             d:DataContext="{d:DesignInstance Type={x:Type local:MenuViewModel}}"
             resolver:DataContextResolver.Type="{x:Type local:MenuViewModel}"
             views:ShellProperty.Title="Menu">
    <Grid>
        <Button Content="Stack" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="75"
                Command="{Binding Forward}" CommandParameter="{x:Type stack:Stack1View}"/>
        <Button Content="Wizard" HorizontalAlignment="Left" Margin="10,35,0,0" VerticalAlignment="Top" Width="75"
            Command="{Binding Forward}" CommandParameter="{x:Type wizard:WizardInput1View}"/>

    </Grid>
</UserControl>
